export default function Background() {
  return (
    <div
      className="absolute inset-0"
      style={{
        background: "radial-gradient(circle at center, #0a1a3a, #000000 70%)"
      }}
    />
  );
}